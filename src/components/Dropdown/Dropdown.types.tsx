type Props = {
  $backgroundColor?: string;
  disabled?: boolean;
  default?: string;
  $items?: string[];
  $onChange?: (val: string) => null;
  className?: string;
  id?: string;
};

export default Props;
